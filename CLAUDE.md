# OpenDuck Mini — 이 저장소에서 작업할 때

## 그래프를 그리기 전에 반드시

**`docs/graph_conventions.md`를 먼저 읽어라.** 2026-07-30에 그린 v28 그래프 4장에서
실제로 어긋난 것들(패널마다 다른 y축, 좌우 관절 부호 반전, 레퍼런스 누락, 글자 겹침,
캡션에 새어나온 튜플, 한글/영문 혼용)을 근거로 쓴 규약이다.

핵심만 옮기면:

- 그리기 전에 `~/Desktop/openduck_graphs/`를 `ls` 한다. 사용자는 새 그래프를
  **기존 30장과 나란히** 본다. 파일명은 `v{번호}_{한글설명}.png`.
- 같은 물리량 패널은 `sharey=True`. 백분위 띠가 축을 지배하게 두지 말 것.
- **실선 = 정책 / 파선 = 레퍼런스**(같은 색, `alpha=0.6`). 모방 학습이므로
  레퍼런스 없는 그래프는 절반만 말한다.
- 제목에 숫자를 박는다. RMS는 rad과 도(°)를 함께.
- **저장한 뒤 그림을 직접 열어서 보라.** 글자 겹침과 캡션 버그는 코드로는 안 보인다.
- 참조 구현: `~/Desktop/openduck_graphs/plot_gait_compare.py`, `plot_periodicity.py`
  (v12~v25 그래프 30장이 여기서 나왔다).

## 맥락

이 저장소는 2026-07-30에 랩PC에서 이 PC로 옮겨왔다. 그 전 3일간의 실험 경과,
무엇이 통했고 무엇이 헛수고였는지는 `docs/handoff/README.md`와
`docs/training_log.md`에 있다. 새 방향을 잡기 전에 읽을 것 — 리워드 계수를
만지려는 충동이 들면 특히.
